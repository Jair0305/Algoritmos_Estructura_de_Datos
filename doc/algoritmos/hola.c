#include <stdio.h> //Agregamos la librería stdio.h que contiene las funciones básicas del lenguaje de programación C.

int main(){ //Se inicializa la función principal del programa.
	
	printf("Hola Mundo"); //Con este código imprimimos en consola el texto deseado.

	return 0; //Retornamos el valor 0 para verificar que el programa se haya completado correctamente.
}